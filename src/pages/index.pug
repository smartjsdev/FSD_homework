include utils/mixins
include utils/variables

    include layout/main
      +page('Main navigation page', 'This page countain the navigation source')
    body.item-page__navigation
      .container
        .nav-wrapper

          ul.navigation|UI kit
            each element in menu_ui
              li.navigation__item
                a(href=element.url) #{'Page ' + i++ + ': ' + element.text}         

          ul.navigation|Website pages
            each element in menu_landing
              li.navigation__item
                a(href=element.url) #{'Page ' + i++ + ': ' + element.text}