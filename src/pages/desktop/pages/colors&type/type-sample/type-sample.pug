mixin type-sample(TypeName, TypeDesc, TypeTag= 'h4', TypeMode = [])
    #{TypeTag}.type-sample.type__type-sample
        //- include __type-name/type-sample__type-name
        //- +type-name(TypeName)
        include ../title/title
        +title(TypeName)
        +title(TypeTag, TypeDesc)
        //- include __type-desc/type-sample__type-desc
        //- +type-desc(TypeDesc, TypeMode)